---
layout: base
title: Projects
description: Translational biology and machine learning projects by Kelvin (Chiu Ho) Mo.
permalink: "/projects/"
---
<section class="section page-intro" aria-labelledby="projects-title">
  <div class="section-head">
    <h1 id="projects-title">Projects</h1>
  </div>
  <p class="page-lede">From translational cancer immunology to biologics manufacturing, I build systems that connect data, decisions, and teams.</p>
</section>

<section class="section" aria-label="Project list">
  <div class="grid-sleek">
    {% for project in profile.projects %}
      {% set title = project.title %}
      {% set org = project.org %}
      {% set timeline = project.timeline %}
      {% set summary = project.summary %}
      {% set outcome = project.outcome %}
      {% set badge = project.badge %}
      {% set tags = project.tags %}
      {% set link = project.link %}
      {% include "components/project-item.njk" %}
    {% endfor %}
  </div>
</section>
