---
layout: base
title: Publications & Awards
description: Selected publications and recognitions by Kelvin (Chiu Ho) Mo.
permalink: "/publications/"
---
<section class="section page-intro" aria-labelledby="pubs-title">
  <div class="section-head">
    <h1 id="pubs-title">Publications & Awards</h1>
  </div>
  <p class="page-lede">Curated work with plain-English summaries so collaborators, clinicians, and students can grasp the impact quickly.</p>
</section>

<section class="section" aria-label="Publication list">
  <div class="pub-list">
    {% for item in profile.publications %}
      {% set title = item.title %}
      {% set venue = item.venue %}
      {% set year = item.year %}
      {% set blurb = item.blurb %}
      {% set contribution = item.contribution %}
      {% set link = item.link %}
      {% include "components/pub-sleek.njk" %}
    {% endfor %}
  </div>
</section>
